# Variáveis de ambiente e segredos
.env
*.env.local

# Dados sensíveis do Hop (credenciais salvas)
docker/hop/.hop/

# Arquivos temporários do sistema
Thumbs.db
.DS_Store

# Logs
*.log

# Drivers JDBC (opcional - alguns preferem não versionar .jar)
# docker/hop/lib/*.jar

# Backups do Power BI
powerbi/*.pbix.bak

# IDE
.vscode/
.idea/

# Conexões do Hop (contêm credenciais ofuscadas)
hop/metadata/rdbms/

# Backups locais do PostgreSQL
backup/

# Backups automaticos (.bak) gerados pelo script 02
*.bak

# Caches regeneraveis do script 02 (grandes, recriados ao rodar o script)
hop/dados_referencia/scripts/cache_visitas_janela.json
hop/dados_referencia/scripts/cache_datas_criacao.json

# Saidas temporarias de consultas (resultados do psql em .txt)
tmp/
zeros.txt
volume.txt
contagens.txt
sequencias.txt